services:
  - type: web
    name: talentscout-assistant
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run version_3.py --server.port $PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false
